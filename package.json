{
  "name": "air-design-monorepo",
  "private": true,
  "description": "AirDesign Monorepo - 通用 UI 组件库 + 业务前端 SDK",
  "workspaces": [
    "packages/*",
    "example"
  ],
  "scripts": {
    "build": "npm run build -w air-design && npm run build -w air-sdk",
    "build:design": "npm run build -w air-design",
    "build:sdk": "npm run build -w air-sdk",
    "dev:example": "npm run dev -w air-design-example"
  }
}
